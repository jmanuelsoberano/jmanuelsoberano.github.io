---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import videosData from '../data/videos.json';

const articles = await getCollection('articles', ({ data }) => !data.draft);
const projects = await getCollection('projects');

// Get latest articles (3)
const latestArticles = articles
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 3);

// Get featured projects (2)
const featuredProjects = projects
  .filter(p => p.data.featured)
  .slice(0, 2);

// Get latest videos (4)
const allVideos = videosData.series.flatMap(s => 
  s.videos.map(v => ({ ...v, seriesTitle: s.title, seriesId: s.id }))
);
const latestVideos = allVideos
  .sort((a, b) => new Date(b.pubDate).valueOf() - new Date(a.pubDate).valueOf())
  .slice(0, 4);
---

<BaseLayout title="Inicio" description="Manuel Soberano - Desarrollador de software, emprendedor y entusiasta por nuevas tecnologías.">
  <section class="hero">
    <div class="hero-layout">
      <div class="hero-text">
        <h1 class="hero-title">Manuel Soberano</h1>
        <div class="hero-bio">
          <p>
            Soy desarrollador de software y me entusiasma la tecnología, pero me importa más lo que sigue siendo válido aunque la tecnología cambie, los fundamentos: valores, principios y buenas prácticas que ayudan a construir software de calidad.
          </p>
          <p>
            Aquí comparto lo que he aprendido de grandes profesionales, libros y experiencia construyendo sistemas, intentando aterrizar esas ideas para que sean útiles en proyectos reales: desde arquitectura y diseño, hasta automatización y calidad del código.
          </p>
        </div>
        <div class="hero-cta">
          <a href="/articulos" class="btn btn-primary">Ver artículos</a>
          <a href="/videos" class="btn btn-secondary">Ver videos</a>
        </div>
      </div>
      <div class="hero-image-container">
        <img 
          src="/images/profile.jpg" 
          alt="Manuel Soberano" 
          class="hero-avatar"
          width="300"
          height="300"
        />
      </div>
    </div>
  </section>

  <section class="section">
    <div class="section-header">
      <h2>Últimos Artículos</h2>
      <a href="/articulos" class="section-link">Ver todos →</a>
    </div>
    <div class="articles-grid">
      {latestArticles.length > 0 ? (
        latestArticles.map(article => (
          <article class="article-card card">
            <div class="article-meta">
              <time datetime={article.data.pubDate.toISOString()}>
                {article.data.pubDate.toLocaleDateString('es-ES', { 
                  year: 'numeric', 
                  month: 'long', 
                  day: 'numeric' 
                })}
              </time>
            </div>
            <h3>
              <a href={`/articulos/${article.slug}`}>{article.data.title}</a>
            </h3>
            <p>{article.data.description}</p>
            <div class="tags">
              {article.data.tags.map(tag => (
                <span class="tag">{tag}</span>
              ))}
            </div>
          </article>
        ))
      ) : (
        <p class="empty-state">Próximamente más artículos...</p>
      )}
    </div>
  </section>

  <section class="section">
    <div class="section-header">
      <h2>Proyectos Destacados</h2>
      <a href="/proyectos" class="section-link">Ver todos →</a>
    </div>
    <div class="projects-grid">
      {featuredProjects.length > 0 ? (
        featuredProjects.map(project => (
          <article class="project-card card">
            <h3>
              <a href={`/proyectos/${project.slug}`}>{project.data.title}</a>
            </h3>
            <p>{project.data.description}</p>
            <div class="stack">
              {project.data.stack.map(tech => (
                <span class="tag">{tech}</span>
              ))}
            </div>
          </article>
        ))
      ) : (
        <p class="empty-state">Próximamente más proyectos...</p>
      )}
    </div>
  </section>

  <section class="section">
    <div class="section-header">
      <h2>Videos Recientes</h2>
      <a href="/videos" class="section-link">Ver todos →</a>
    </div>
    <div class="videos-grid">
      {latestVideos.map(video => (
        <article class="video-card card">
          <a 
            href={`https://www.youtube.com/watch?v=${video.videoId}`} 
            target="_blank" 
            rel="noopener noreferrer"
            class="video-thumbnail"
          >
            <img 
              src={`https://img.youtube.com/vi/${video.videoId}/mqdefault.jpg`} 
              alt={video.title}
              loading="lazy"
              width="320"
              height="180"
            />
            <div class="play-overlay">
              <svg width="48" height="48" viewBox="0 0 24 24" fill="white">
                <path d="M8 5v14l11-7z"/>
              </svg>
            </div>
          </a>
          <div class="video-info">
            <span class="video-series">{video.seriesTitle}</span>
            <h3>
              <a 
                href={`https://www.youtube.com/watch?v=${video.videoId}`} 
                target="_blank" 
                rel="noopener noreferrer"
              >
                {video.title}
              </a>
            </h3>
          </div>
        </article>
      ))}
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    padding: var(--space-8) var(--space-6);
    background: var(--bg-secondary);
    border-bottom: 1px solid var(--border);
  }

  .hero-layout {
    max-width: var(--content-wide); /* Wider content for 2 cols */
    margin: 0 auto;
    display: flex;
    flex-direction: column-reverse; /* Text bottom, image top on mobile */
    gap: var(--space-8);
    align-items: center;
    text-align: center;
  }

  /* Desktop Layout */
  @media (min-width: 768px) {
    .hero {
      padding: var(--space-16) var(--space-6);
    }
    
    .hero-layout {
      display: grid;
      grid-template-columns: 3fr 2fr; /* Text gets more space */
      align-items: center;
      text-align: left;
      gap: var(--space-12);
    }
    
    .hero-cta {
      justify-content: flex-start;
    }
  }
  
  .hero-image-container {
    display: flex;
    justify-content: center;
  }

  .hero-avatar {
    width: 200px;
    height: 200px;
    border-radius: 50%;
    object-fit: cover;
    border: 4px solid var(--bg-primary);
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  }

  @media (min-width: 768px) {
    .hero-avatar {
      width: 280px;
      height: 280px;
    }
  }
  
  .hero-title {
    font-size: var(--text-4xl);
    margin-bottom: var(--space-4);
    line-height: 1.1;
  }
  
  @media (min-width: 768px) {
    .hero-title {
       font-size: var(--text-5xl);
    }
  }

  .hero-bio {
    font-size: var(--text-lg);
    line-height: 1.7;
    color: var(--text-secondary);
    margin-bottom: var(--space-8);
  }
  
  .hero-bio p {
    margin-bottom: var(--space-4);
  }

  .hero-cta {
    display: flex;
    gap: var(--space-4);
    justify-content: center; /* Centered on mobile */
    flex-wrap: wrap;
  }
  
  .section {
    max-width: var(--content-full);
    margin: 0 auto;
    padding: var(--space-12) var(--space-6);
  }
  
  .section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--space-8);
  }
  
  .section-header h2 {
    margin: 0;
  }
  
  .section-link {
    font-size: var(--text-sm);
    font-weight: 500;
  }
  
  .articles-grid {
    display: grid;
    gap: var(--space-6);
  }
  
  @media (min-width: 768px) {
    .articles-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  
  .article-card h3 {
    font-size: var(--text-xl);
    margin: var(--space-3) 0;
  }
  
  .article-card h3 a {
    color: var(--text-primary);
    text-decoration: none;
  }
  
  .article-card h3 a:hover {
    color: var(--accent);
  }
  
  .article-meta {
    font-size: var(--text-sm);
    color: var(--text-muted);
  }
  
  .article-card p {
    font-size: var(--text-sm);
    margin-bottom: var(--space-4);
  }
  
  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }
  
  .projects-grid {
    display: grid;
    gap: var(--space-6);
  }
  
  @media (min-width: 768px) {
    .projects-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  .project-card h3 {
    font-size: var(--text-xl);
    margin-bottom: var(--space-3);
  }
  
  .project-card h3 a {
    color: var(--text-primary);
    text-decoration: none;
  }
  
  .project-card h3 a:hover {
    color: var(--accent);
  }
  
  .project-card p {
    margin-bottom: var(--space-4);
  }
  
  .stack {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }
  
  .videos-grid {
    display: grid;
    gap: var(--space-6);
  }
  
  @media (min-width: 640px) {
    .videos-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  @media (min-width: 1024px) {
    .videos-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  
  .video-card {
    padding: 0;
    overflow: hidden;
  }
  
  .video-thumbnail {
    position: relative;
    display: block;
  }
  
  .video-thumbnail img {
    width: 100%;
    height: auto;
    transition: transform var(--transition-normal);
  }
  
  .video-thumbnail:hover img {
    transform: scale(1.05);
  }
  
  .play-overlay {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background: rgba(0, 0, 0, 0.6);
    border-radius: 50%;
    width: 56px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0.8;
    transition: opacity var(--transition-fast);
  }
  
  .video-thumbnail:hover .play-overlay {
    opacity: 1;
  }
  
  .video-info {
    padding: var(--space-4);
  }
  
  .video-series {
    font-size: var(--text-xs);
    color: var(--accent);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  
  .video-info h3 {
    font-size: var(--text-sm);
    margin-top: var(--space-2);
    line-height: 1.4;
  }
  
  .video-info h3 a {
    color: var(--text-primary);
    text-decoration: none;
  }
  
  .video-info h3 a:hover {
    color: var(--accent);
  }
  
  .empty-state {
    color: var(--text-muted);
    font-style: italic;
  }
</style>
